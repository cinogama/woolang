import rscene.std;
import test_tool;

namespace test_dup
{
    func main()
    {
        var arr = [1];
        var dup_arr = arr->dup();

        var map = {{"key", "val"}};
        var dup_map = map->dup();

        dup_arr[0] = 233;
        dup_map["key"] = "modify";

        test_equal(arr[0], 1);
        test_equal(map["key"], "val");
    }
}

test_function("test_dup.main", test_dup::main);