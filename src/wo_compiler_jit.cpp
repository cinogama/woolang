#include "wo_afx.hpp"

#include "wo_compiler_jit.hpp"

namespace wo
{
    struct runtime_env;
    void update_env_jit(runtime_env* env)
    {
    }

    void cleanup_env_jit(runtime_env* env)
    {
    }
}